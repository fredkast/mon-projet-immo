{% extends 'base.html.twig' %}

{% block title %}Devis{% endblock %}

{% block body %}
    <h1>Devis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ devi.id }}</td>
            </tr>
            <tr>
                <th>Type_maison</th>
                <td>{{ devi.typeMaison }}</td>
            </tr>
            <tr>
                <th>Type_usage</th>
                <td>{{ devi.typeUsage }}</td>
            </tr>
            <tr>
                <th>Surface</th>
                <td>{{ devi.surface }}</td>
            </tr>
            <tr>
                <th>Budget</th>
                <td>{{ devi.budget }}</td>
            </tr>
            <tr>
                <th>Nombre_etages</th>
                <td>{{ devi.nombreEtages }}</td>
            </tr>
            <tr>
                <th>Nombre_chambres</th>
                <td>{{ devi.nombreChambres }}</td>
            </tr>
            <tr>
                <th>Garage</th>
                <td>{{ devi.garage ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Exterieur</th>
                <td>{{ devi.exterieur ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('devis_index') }}">back to list</a>

    <a href="{{ path('devis_edit', {'id': devi.id}) }}">edit</a>

    {{ include('devis/_delete_form.html.twig') }}
{% endblock %}
